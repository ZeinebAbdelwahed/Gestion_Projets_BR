A# ================================
# Base de donnees — GCP Cloud SQL
# Remplace les valeurs <...> par les tiennes (console GCP)
# ================================
spring.datasource.url=jdbc:mysql:///gestion_projets?cloudSqlInstance=gestionprojetbr:europe-west9:gestion-projet-db&socketFactory=com.google.cloud.sql.mysql.SocketFactory
spring.datasource.username=zeineb
spring.datasource.password=zein95A
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate — tables deja creees via schema.sql
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# CORS — remplace par ton URL App Engine / Cloud Run
app.cors.allowed-origins=https://gestionprojetbr.appspot.com,http://localhost:4200