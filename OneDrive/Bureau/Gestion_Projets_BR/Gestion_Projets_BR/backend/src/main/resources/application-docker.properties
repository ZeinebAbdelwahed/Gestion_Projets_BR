# ================================
# Base de données (profil Docker)
# ================================
spring.datasource.url=jdbc:mysql://mysql:3306/gestion_projets?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# ================================
# CORS (profil Docker)
# ================================
app.cors.allowed-origins=http://localhost,http://localhost:80
